<h1>Node migration script</h1>
<p>This assignment was created for edx. The script merges the data from two JSON files (made for customer data). <br/>
the data is then inserted to Mongo in parallel. You can change the amount of insertions through ecah function, through the command line by:</br>
node migrate-data [number of objects per function] or node migrate-data 50. 
